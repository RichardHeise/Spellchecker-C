#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main () {
    char *entrada;
    int tamanho;

    tamanho = 1000;
    malloc (tamanho * sizeof (char));

    readDict(entrada);

    
}
